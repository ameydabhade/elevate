# Elevate^

A modern portfolio website built with Framer and optimized for Vercel deployment.

## 🚀 Quick Deploy

Deploy this project to Vercel with one click:

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/YOUR_USERNAME/YOUR_REPO_NAME)

## 📁 Project Structure

```
Elevate/
├── index.html          # Main HTML file (exported from Framer)
├── vercel.json         # Vercel deployment configuration
├── package.json        # Project metadata
└── README.md          # Project documentation
```

## 🛠️ Local Development

To run the project locally:

```bash
# Using Python (built-in)
python3 -m http.server 3000

# Using Node.js (if you have it installed)
npx serve .

# Using Live Server (VS Code extension)
# Right-click on index.html and select "Open with Live Server"
```

Then open [http://localhost:3000](http://localhost:3000) in your browser.

## 🚀 Deployment

### Vercel (Recommended)

1. Connect your GitHub repository to Vercel
2. Vercel will automatically detect the static site configuration
3. Deploy with zero configuration needed

### Other Platforms

This is a static HTML site that can be deployed to any static hosting service:
- Netlify
- GitHub Pages
- Firebase Hosting
- AWS S3 + CloudFront

## 📝 Notes

- This project was exported from Framer
- All assets are hosted externally (Framer CDN)
- No build process required - it's a static site
- Optimized for modern browsers with responsive design
